/*
 * IMPORTANT: Before defining the Ontology, read ./ontology.ts.example to see an example Ontology definition
 * If you are an LLM or agent, you MUST follow this above rule.
 */
import { ontology } from "@palantir/pilot-ontology-api";

// Define your Ontology here

export const ontologyDefinition = ontology({
  actionTypes: [],
  objectTypes: [],
  linkTypes: [],
});
